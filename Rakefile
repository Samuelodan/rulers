# frozen_string_literal: true

require "bundler/gem_tasks"
# require "rspec/core/rake_task" // temporarily replacing with minitest

# RSpec::Core::RakeTask.new(:spec) // temporarily replacing with minitest

require "standard/rake"

task default: %i[spec standard]
